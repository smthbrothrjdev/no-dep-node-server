{
  "name": "repo",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && node dist/copyPublic.js",
    "watch": "tsc --watch",
    "start": "node dist/index.js",
    "dev": "sh -c 'npm run build -- --watch & node --watch --watch-path=dist dist/index.js'",
    "start:perf": "node dist/index.js --metrics"
	},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.2.1",
    "typescript": "^5.9.2"
  }
}
